---
name: Build
on:
  push:
    paths:
      - "libft/**"
      - "src/**"
      - "include/**"
      - ".github/workflows/**"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: make
